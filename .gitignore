# Font build workspace — keep the build script, ignore the venv
font-build/venv/
font-build/__pycache__/
font-build/*.pyc

# macOS
.DS_Store
__MACOSX/

# Secrets — never commit
.env
.env.local
tools/output/
