# Font build workspace — keep the build script, ignore the venv
font-build/venv/
font-build/__pycache__/
font-build/*.pyc

# macOS
.DS_Store
__MACOSX/

# Secrets — never commit
.env
.env.local
tools/output/

# Heavy non-live assets — keep local, keep out of the deployed site (GitHub Pages 1 GB limit)
images/not-in-use/
Preview/

# Source video masters — converted into vid/*.mp4, kept out of the repo (delete the local copies when convenient)
videos/mo_cap_graffiti_wall_for_panorama_festival_v1 (1080p).mp4
images/og-panorama-festival-2016.jpg

# Panorama photo source originals — optimized into images/panorama-*.webp, keep originals local only
images/Panorama/
